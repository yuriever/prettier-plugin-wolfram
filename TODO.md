# TODO

## Parser

* [ ] Restore parser reproducibility: either remove the unavailable `grammar` source submodule or update it to an obtainable upstream revision, then regenerate and test the committed `src/parser/tree-sitter-wolfram.wasm`.
